[gd_scene load_steps=9 format=2]

[ext_resource path="res://assets/ink/placeholder_antibodies.ink" type="Resource" id=1]
[ext_resource path="res://assets/scripts/environment/interactive.gd" type="Script" id=2]
[ext_resource path="res://assets/scripts/environment/shiftable.gd" type="Script" id=3]
[ext_resource path="res://assets/sprites/objects/sheet_warmSubject_real.png" type="Texture" id=4]

[sub_resource type="Shader" id=24]
code = "shader_type canvas_item;

uniform vec4 color : hint_color = vec4(1.0);
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;

void vertex() {

}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;
		
		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}
		
		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);
			
			if ((xy != clamp(xy, vec2(0.0), vec2(1.0)) || texture(texture, xy).a == 0.0) == inside) {
				return true;
			}
		}
	}
	
	return false;
}

void fragment() {
	vec2 uv = UV;
	COLOR = texture(TEXTURE, uv);

	
	if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, TEXTURE)) {
		COLOR.rgb = inside ? mix(COLOR.rgb, color.rgb, color.a) : color.rgb;
		COLOR.a += (1.0 - COLOR.a) * color.a;
	}
}"

[sub_resource type="ShaderMaterial" id=25]
shader = SubResource( 24 )
shader_param/color = Color( 1, 0.605469, 0.605469, 1 )
shader_param/width = 1.0
shader_param/pattern = 0
shader_param/inside = false

[sub_resource type="RectangleShape2D" id=23]
extents = Vector2( 16, 8 )

[sub_resource type="RectangleShape2D" id=22]
extents = Vector2( 21, 12 )

[node name="OBJECT" type="RigidBody2D" groups=["shiftable"]]
mode = 1
script = ExtResource( 3 )
__meta__ = {
"_editor_description_": ""
}
dreamsheet = ExtResource( 4 )
realsheet = ExtResource( 4 )

[node name="Sprite" type="Sprite" parent="."]
light_mask = 4
material = SubResource( 25 )
position = Vector2( 0, -19 )
texture = ExtResource( 4 )
region_enabled = true
region_rect = Rect2( 143, 137, 34, 40 )

[node name="Hint" type="Sprite" parent="."]
modulate = Color( 1, 1, 1, 0.313726 )
show_behind_parent = true
light_mask = 4
texture = ExtResource( 4 )
offset = Vector2( 0, -20 )
region_enabled = true
region_rect = Rect2( 143, 137, 34, 40 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2( 0, -8 )
shape = SubResource( 23 )

[node name="ActiveArea" type="Area2D" parent="."]
script = ExtResource( 2 )
__meta__ = {
"_editor_description_": ""
}
inkFileDream = ExtResource( 1 )
inkFileReal = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="ActiveArea"]
position = Vector2( 0, -4 )
shape = SubResource( 22 )

[connection signal="body_entered" from="ActiveArea" to="ActiveArea" method="_on_InteractArea_body_entered"]
[connection signal="body_exited" from="ActiveArea" to="ActiveArea" method="_on_InteractArea_body_exited"]
[connection signal="can_interact" from="ActiveArea" to="." method="_on_ActiveArea_can_interact"]
[connection signal="cannot_interact" from="ActiveArea" to="." method="_on_ActiveArea_cannot_interact"]
